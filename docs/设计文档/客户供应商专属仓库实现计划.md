# 客户/供应商专属仓库 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现客户/供应商专属仓库——成品按工单客户定向入客户仓并硬隔离发货，原料按供应商定向入供应商仓并供外协发料软优先消耗，出入库时可即时新建带名称+地址+归属的仓。

**Architecture:** 隔离边界=仓库。`qxx_wm_warehouse` 加 `client_id`/`vendor_id` + 类型 `CUSTOMER`/`SUPPLIER`，库存桶唯一键已含 `warehouse_id` 故天然分桶，不改库存核心表。产品入库带 `client_id`（来源工单）、采购入库复用已有 `vendorId`。

**Tech Stack:** Spring Boot 4 + MyBatis + Flyway（后端）；Vue 3 + TS + Element Plus（前端）；若依代码生成体系。

## Global Constraints

- 所有 SQL WHERE 带 `factory_id`（拦截器注入值，SQL 仍写 `<if>`）；业务 INSERT **不写** `factory_id`（拦截器注入）；Flyway 内 INSERT **显式写** `factory_id`。
- 后端函数 ≤ 50 行；前端组件 ≤ 300 行；业务状态/类型用常量枚举不用裸字符串。
- API 契约：单条 `{code:200,msg,data}`；字段 camelCase 前后端一致。
- **后端改动验证红线**：`mvn package` + 重启 jar + token 调真实接口实测（非只看日志）；前端改动浏览器实测。
- Flyway 迁移幂等（`INFORMATION_SCHEMA` 判断列存在再 ALTER）；版本号取当前最大 +1（当前最大 V120，预计 **V121**，实现前 `ls db/migration` 确认）。

## File Structure

**新建：**
- `backend/.../db/migration/V121__warehouse_client_vendor.sql` — 加列迁移
- `backend/ruoyi-common/.../enums/WmWarehouseConstants.java` — 仓库类型常量

**后端修改：**
- `WmWarehouse.java` / `WmWarehouseMapper.xml` — 加 client_id/vendor_id
- `WmWarehouseServiceImpl.java` / `IWmWarehouseService.java` — 类型校验 + 按客户/供应商查仓
- `WmProductRecpt.java` / `WmProductRecptMapper.xml` — 加 client_id
- `ProWorkorderDocServiceImpl.java` — 产品入库定向入客户仓
- `WmItemRecptServiceImpl.java` — 采购入库定向入供应商仓
- `WmProductSalesServiceImpl.java` — 销售出库客户仓硬隔离
- `OutsourceIssueHelper.java` — 外协发料软优先供应商仓

**前端修改：**
- `api/mes/wm/warehouse.ts` — 类型枚举同步
- `components/warehouseSelect/single.vue` — 即时新建客户/供应商仓
- `views/mes/wm/warehouse/index.vue` — 客户/供应商仓 CRUD
- `views/mes/wm/productrecpt/index.vue` — 客户字段展示
- `views/mes/wm/product_sales/*` — 发货仓库过滤

---

## Task 1: Flyway 迁移 — 仓库表/产品入库表加归属字段

**Files:**
- Create: `backend/ruoyi-admin/src/main/resources/db/migration/V121__warehouse_client_vendor.sql`

**Interfaces:**
- Produces: DB 列 `qxx_wm_warehouse.client_id`、`qxx_wm_warehouse.vendor_id`、`qxx_wm_product_recpt.client_id`（后续任务的字段映射依赖）

- [ ] **Step 1: 确认最新版本号**

```bash
ls backend/ruoyi-admin/src/main/resources/db/migration/ | sort -V | tail -3
```
Expected: 最大版本若 > V120，则本文件版本号改为「最大+1」（文件名同步改）。下文以 V121 为例。

- [ ] **Step 2: 写迁移文件（幂等 ALTER）**

```sql
-- V121__warehouse_client_vendor.sql
-- 客户/供应商专属仓库：仓库表加归属字段，产品入库表加客户字段

-- 仓库表：客户仓归属
SET @s = IF((SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS
    WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'qxx_wm_warehouse' AND COLUMN_NAME = 'client_id') = 0,
    'ALTER TABLE qxx_wm_warehouse ADD COLUMN client_id bigint(20) DEFAULT NULL COMMENT ''客户仓归属客户ID(warehouse_type=CUSTOMER时必填)'' AFTER warehouse_type',
    'SELECT 1');
PREPARE stmt FROM @s; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- 仓库表：供应商仓归属
SET @s = IF((SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS
    WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'qxx_wm_warehouse' AND COLUMN_NAME = 'vendor_id') = 0,
    'ALTER TABLE qxx_wm_warehouse ADD COLUMN vendor_id bigint(20) DEFAULT NULL COMMENT ''供应商仓归属供应商ID(warehouse_type=SUPPLIER时必填)'' AFTER client_id',
    'SELECT 1');
PREPARE stmt FROM @s; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- 产品入库表：成品归属客户（来源工单）
SET @s = IF((SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS
    WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'qxx_wm_product_recpt' AND COLUMN_NAME = 'client_id') = 0,
    'ALTER TABLE qxx_wm_product_recpt ADD COLUMN client_id bigint(20) DEFAULT NULL COMMENT ''成品归属客户ID(来源工单)'' AFTER workorder_code',
    'SELECT 1');
PREPARE stmt FROM @s; EXECUTE stmt; DEALLOCATE PREPARE stmt;

-- 仓库类型字典 mes_warehouse_type 补 CUSTOMER/SUPPLIER（系统级字典，无 factory_id；dict_code 自增省略，按 dict_value 幂等）
INSERT INTO sys_dict_data (dict_sort, dict_label, dict_value, dict_type, css_class, list_class, is_default, status, create_by, create_time, remark)
SELECT 6, '客户仓', 'CUSTOMER', 'mes_warehouse_type', '', 'success', 'N', '0', 'admin', NOW(), '仓库类型-客户仓'
FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM sys_dict_data WHERE dict_type='mes_warehouse_type' AND dict_value='CUSTOMER');

INSERT INTO sys_dict_data (dict_sort, dict_label, dict_value, dict_type, css_class, list_class, is_default, status, create_by, create_time, remark)
SELECT 7, '供应商仓', 'SUPPLIER', 'mes_warehouse_type', '', 'warning', 'N', '0', 'admin', NOW(), '仓库类型-供应商仓'
FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM sys_dict_data WHERE dict_type='mes_warehouse_type' AND dict_value='SUPPLIER');
```

- [ ] **Step 3: 应用迁移并验证**

```bash
cd backend && mvn -pl ruoyi-admin -am package -DskipTests -q
# 启动会让 Flyway 自动执行 V121；启动后查列
mysql -h127.0.0.1 -P3307 -uroot -p qxx_mes -e "SHOW COLUMNS FROM qxx_wm_warehouse LIKE '%_id'" 2>/dev/null | grep -E "client_id|vendor_id"
```
Expected: 输出含 client_id、vendor_id 两行。

- [ ] **Step 4: Commit**

```bash
git add backend/ruoyi-admin/src/main/resources/db/migration/V121__warehouse_client_vendor.sql
git commit -m "feat(db): V121 仓库表加客户/供应商归属字段 + 产品入库加客户字段"
```

---

## Task 2: 仓库类型常量 + 仓库域类/Mapper 加字段

**Files:**
- Create: `backend/ruoyi-common/src/main/java/com/ruoyi/common/enums/WmWarehouseConstants.java`
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/domain/mes/wm/WmWarehouse.java`
- Modify: `backend/ruoyi-system/src/main/resources/mapper/mes/wm/WmWarehouseMapper.xml`

**Interfaces:**
- Produces: `WmWarehouseConstants.TYPE_CUSTOMER/SUPPLIER`；`WmWarehouse.clientId/vendorId`；Mapper 四处列映射，供 Task 3/5/6/7/8 使用。

- [ ] **Step 1: 新建仓库类型常量类**

```java
package com.ruoyi.common.enums;

/**
 * 仓库类型常量。warehouse_type 取值。
 */
public final class WmWarehouseConstants {
    public static final String TYPE_RAW = "RAW";             // 原料仓
    public static final String TYPE_FINISHED = "FINISHED";   // 成品仓
    public static final String TYPE_AUX = "AUX";             // 辅料仓
    public static final String TYPE_LINE = "LINE";           // 线边库
    public static final String TYPE_TEMP = "TEMP";           // 临时仓
    public static final String TYPE_CUSTOMER = "CUSTOMER";   // 客户仓(归属客户)
    public static final String TYPE_SUPPLIER = "SUPPLIER";   // 供应商仓(归属供应商)

    private WmWarehouseConstants() {}
}
```

- [ ] **Step 2: WmWarehouse.java 加字段（在 warehouseType 字段后，约行 33 后）**

在 `private String warehouseType;` 之后插入：
```java
    /** 客户仓归属客户ID(warehouse_type=CUSTOMER时必填) */
    private Long clientId;
    /** 供应商仓归属供应商ID(warehouse_type=SUPPLIER时必填) */
    private Long vendorId;
```
在 getter/setter 区（warehouseType 的 getter/setter 后，约行 60 后）加：
```java
    public Long getClientId() { return clientId; }
    public void setClientId(Long v) { this.clientId = v; }
    public Long getVendorId() { return vendorId; }
    public void setVendorId(Long v) { this.vendorId = v; }
```

- [ ] **Step 3: WmWarehouseMapper.xml resultMap 加映射（warehouseType 行后）**

在 `<result property="warehouseType" column="warehouse_type" />` 后加：
```xml
    <result property="clientId" column="client_id" />
    <result property="vendorId" column="vendor_id" />
```

- [ ] **Step 4: selectWmWarehouseVo 列清单加两列**

```xml
<sql id="selectWmWarehouseVo">
    select warehouse_id, factory_id, warehouse_code, warehouse_name, warehouse_type, client_id, vendor_id, address, area, charge, enable_flag, remark, create_by, create_time, update_by, update_time from qxx_wm_warehouse
</sql>
```

- [ ] **Step 5: selectWmWarehouseList where 加精确匹配**

在 `<if test="warehouseType ...">` 后加（精确匹配，不用 like）：
```xml
        <if test="clientId != null"> and client_id = #{clientId}</if>
        <if test="vendorId != null"> and vendor_id = #{vendorId}</if>
```

- [ ] **Step 6: insertWmWarehouse 字段列表+值列表各加两行**

字段列表 `<trim prefix="("...>` 在 warehouseType 行后加：
```xml
        <if test="clientId != null">client_id,</if>
        <if test="vendorId != null">vendor_id,</if>
```
值列表 `<trim prefix="values ("...>` 在 warehouseType 行后加：
```xml
        <if test="clientId != null">#{clientId},</if>
        <if test="vendorId != null">#{vendorId},</if>
```

- [ ] **Step 7: updateWmWarehouse SET 加两行**

在 warehouseType 行后加：
```xml
        <if test="clientId != null">client_id = #{clientId},</if>
        <if test="vendorId != null">vendor_id = #{vendorId},</if>
```

- [ ] **Step 8: 编译验证**

```bash
cd backend && mvn -pl ruoyi-common,ruoyi-system -am compile -q
```
Expected: BUILD SUCCESS。

- [ ] **Step 9: Commit**

```bash
git add backend/ruoyi-common/src/main/java/com/ruoyi/common/enums/WmWarehouseConstants.java \
        backend/ruoyi-system/src/main/java/com/ruoyi/system/domain/mes/wm/WmWarehouse.java \
        backend/ruoyi-system/src/main/resources/mapper/mes/wm/WmWarehouseMapper.xml
git commit -m "feat(wm/warehouse): 仓库域类+Mapper加客户/供应商归属字段"
```

---

## Task 3: 仓库 Service 类型校验 + 按客户/供应商查仓

**Files:**
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/IWmWarehouseService.java`
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/impl/WmWarehouseServiceImpl.java`

**Interfaces:**
- Consumes: `WmWarehouseConstants.TYPE_CUSTOMER/SUPPLIER`（Task 2）
- Produces:
  - `WmWarehouse findClientWarehouse(Long factoryId, Long clientId)` — 返回该工厂下该客户的客户仓（无则 null）
  - `WmWarehouse findVendorWarehouse(Long factoryId, Long vendorId)` — 返回该工厂下该供应商的供应商仓（无则 null）
  - `insertWmWarehouse`/`updateWmWarehouse` 内联类型/归属校验

- [ ] **Step 1: IWmWarehouseService 加两个查仓方法**

在接口方法列表加：
```java
    /** 查某工厂下某客户的专属客户仓（无则返回 null） */
    WmWarehouse findClientWarehouse(Long factoryId, Long clientId);
    /** 查某工厂下某供应商的专属供应商仓（无则返回 null） */
    WmWarehouse findVendorWarehouse(Long factoryId, Long vendorId);
```

- [ ] **Step 2: WmWarehouseServiceImpl 加 import 和校验方法**

文件顶部 import 区加：
```java
import com.ruoyi.common.enums.WmWarehouseConstants;
import com.ruoyi.common.exception.ServiceException;
```
在 `insertWmWarehouse` 方法体的 `entity.setCreateTime(...)` 前加：
```java
        normalizeWarehouseOwner(entity);
```
在 `updateWmWarehouse` 方法体的 `entity.setUpdateTime(...)` 前加：
```java
        normalizeWarehouseOwner(entity);
```
在类内加私有方法：
```java
    /** 校验并归一仓库归属：客户仓必填 clientId、供应商仓必填 vendorId、普通仓清空两者。 */
    private void normalizeWarehouseOwner(WmWarehouse w) {
        String t = w.getWarehouseType();
        if (WmWarehouseConstants.TYPE_CUSTOMER.equals(t)) {
            if (w.getClientId() == null) {
                throw new ServiceException("客户仓必须填写归属客户");
            }
            w.setVendorId(null);
        } else if (WmWarehouseConstants.TYPE_SUPPLIER.equals(t)) {
            if (w.getVendorId() == null) {
                throw new ServiceException("供应商仓必须填写归属供应商");
            }
            w.setClientId(null);
        } else {
            w.setClientId(null);
            w.setVendorId(null);
        }
    }
```

- [ ] **Step 3: 实现两个查仓方法（类内）**

```java
    @Override
    public WmWarehouse findClientWarehouse(Long factoryId, Long clientId) {
        if (clientId == null) return null;
        WmWarehouse q = new WmWarehouse();
        q.setFactoryId(factoryId);
        q.setWarehouseType(WmWarehouseConstants.TYPE_CUSTOMER);
        q.setClientId(clientId);
        q.setEnableFlag("1");
        List<WmWarehouse> list = wmWarehouseMapper.selectWmWarehouseList(q);
        return (list != null && !list.isEmpty()) ? list.get(0) : null;
    }

    @Override
    public WmWarehouse findVendorWarehouse(Long factoryId, Long vendorId) {
        if (vendorId == null) return null;
        WmWarehouse q = new WmWarehouse();
        q.setFactoryId(factoryId);
        q.setWarehouseType(WmWarehouseConstants.TYPE_SUPPLIER);
        q.setVendorId(vendorId);
        q.setEnableFlag("1");
        List<WmWarehouse> list = wmWarehouseMapper.selectWmWarehouseList(q);
        return (list != null && !list.isEmpty()) ? list.get(0) : null;
    }
```

- [ ] **Step 4: 编译 + 重启 + 实测**

```bash
cd backend && mvn -pl ruoyi-admin -am package -DskipTests -q
# kill 旧进程后重启
TOKEN=$(python3 backend/scripts/get_token.py 2>/dev/null)
# 建一个缺归属的客户仓，应被拦截
curl -s -X POST http://localhost:8081/mes/wm/warehouse -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"warehouseCode":"TEST_CUST_BAD","warehouseName":"测试无归属客户仓","warehouseType":"CUSTOMER","factoryId":1}'
```
Expected: 返回 `code:500` 且 msg 含「客户仓必须填写归属客户」。

- [ ] **Step 5: Commit**

```bash
git add backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/IWmWarehouseService.java \
        backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/impl/WmWarehouseServiceImpl.java
git commit -m "feat(wm/warehouse): 仓库类型/归属校验 + 按客户/供应商查专属仓"
```

---

## Task 4: 产品入库域类/Mapper 加 client_id

**Files:**
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/domain/mes/wm/WmProductRecpt.java`
- Modify: `backend/ruoyi-system/src/main/resources/mapper/mes/wm/WmProductRecptMapper.xml`

**Interfaces:**
- Produces: `WmProductRecpt.clientId`（Task 5 写入）

- [ ] **Step 1: WmProductRecpt.java 加字段（workorderCode 字段后）**

```java
    /** 成品归属客户ID(来源工单) */
    private Long clientId;
```
getter/setter：
```java
    public Long getClientId() { return clientId; }
    public void setClientId(Long v) { this.clientId = v; }
```

- [ ] **Step 2: WmProductRecptMapper.xml resultMap 加映射**

在 workorder_code 映射后加：
```xml
    <result property="clientId" column="client_id" />
```

- [ ] **Step 3: selectWmProductRecptVo 列清单加 client_id**

（参照 WmWarehouse 的改法，在 select 列表加 `client_id`）

- [ ] **Step 4: insertWmProductRecpt / updateWmProductRecpt 加 client_id**

insert 字段列表+值列表加：
```xml
        <if test="clientId != null">client_id,</if>
```
```xml
        <if test="clientId != null">#{clientId},</if>
```
update SET 加：
```xml
        <if test="clientId != null">client_id = #{clientId},</if>
```

- [ ] **Step 5: 编译验证**

```bash
cd backend && mvn -pl ruoyi-system -am compile -q
```
Expected: BUILD SUCCESS。

- [ ] **Step 6: Commit**

```bash
git add backend/ruoyi-system/src/main/java/com/ruoyi/system/domain/mes/wm/WmProductRecpt.java \
        backend/ruoyi-system/src/main/resources/mapper/mes/wm/WmProductRecptMapper.xml
git commit -m "feat(wm/product_recpt): 产品入库加客户归属字段"
```

---

## Task 5: 产品入库定向入客户仓

**Files:**
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/pro/impl/ProWorkorderDocServiceImpl.java`（方法 `generateReceiptDocuments` 行 675-766，重点行 722 `findDefaultWarehouse` 调用处）

**Interfaces:**
- Consumes: `wmWarehouseService.findClientWarehouse(factoryId, clientId)`（Task 3）；`ProWorkorder.getClientId()`（已存在）；`WmProductRecpt.setClientId`（Task 4）

- [ ] **Step 1: 改造 generateReceiptDocuments 的仓库选择**

定位行 722 附近：
```java
    Long defaultWarehouseId = findDefaultWarehouse(wo.getFactoryId());
```
替换为：
```java
    Long warehouseId = resolveRecptWarehouseId(wo);
```
并在构造 `recpt` 后（`recpt.setWarehouseId(defaultWarehouseId);` 处）把两处 `defaultWarehouseId` 引用改为 `warehouseId`，并加客户：
```java
    recpt.setClientId(wo.getClientId());
    recpt.setWarehouseId(warehouseId);
```
recptLine 的 `setWarehouseId(defaultWarehouseId)` 同改 `warehouseId`。

- [ ] **Step 2: 新增私有方法 resolveRecptWarehouseId（类内）**

```java
    /**
     * 解析产品入库目标仓：工单有客户且该客户有专属客户仓→入客户仓；否则回退工厂默认仓。
     * 决策C：客户无专属仓时不自动建，入公共成品仓（前端引导新建）。
     */
    private Long resolveRecptWarehouseId(ProWorkorder wo) {
        if (wo.getClientId() != null) {
            WmWarehouse clientWh = wmWarehouseService.findClientWarehouse(wo.getFactoryId(), wo.getClientId());
            if (clientWh != null) return clientWh.getWarehouseId();
        }
        return findDefaultWarehouse(wo.getFactoryId());
    }
```
（确认 `wmWarehouseService` 已在该类注入——`findDefaultWarehouse` 已使用，故已注入。）

- [ ] **Step 3: 确认 WmWarehouse 已 import**

类顶部 import：
```java
import com.ruoyi.system.domain.mes.wm.WmWarehouse;
```
（若已存在则跳过。）

- [ ] **Step 4: 编译 + 重启 + 实测**

```bash
cd backend && mvn -pl ruoyi-admin -am package -DskipTests -q
# 重启后：准备一个 clientId 非空且已建客户仓的工单，触发报工审核/手工生成入库单
TOKEN=$(python3 backend/scripts/get_token.py 2>/dev/null)
curl -s "http://localhost:8081/mes/pro/workorder/doc/genReceipt?workorderId=<带客户的工单ID>" -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
# 查生成的入库单 warehouse_id 是否落在客户仓、client_id 是否带客户
```
Expected: 入库单 `warehouseId` = 客户仓 ID，`clientId` = 工单客户。

- [ ] **Step 5: Commit**

```bash
git add backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/pro/impl/ProWorkorderDocServiceImpl.java
git commit -m "feat(pro/recpt): 产品入库按工单客户定向入客户仓"
```

---

## Task 6: 采购入库定向入供应商仓

**Files:**
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/impl/WmItemRecptServiceImpl.java`（方法 `buildFromPurOrder` 行 521-545）

**Interfaces:**
- Consumes: `wmWarehouseService.findVendorWarehouse(factoryId, vendorId)`（Task 3）；`PurOrder.getVendorId/getFactoryId`（已存在）

- [ ] **Step 1: 注入 IWmWarehouseService**

类字段区加（若未注入）：
```java
    @Autowired
    private com.ruoyi.system.service.mes.wm.IWmWarehouseService wmWarehouseService;
```

- [ ] **Step 2: buildFromPurOrder 末尾加定向仓库**

在 `return draft;`（约行 544）前插入：
```java
        // 定向入供应商仓（决策E1）：命中供应商专属仓则预填头仓库，未命中保持空由用户头表选
        if (order.getVendorId() != null) {
            WmWarehouse vendorWh = wmWarehouseService.findVendorWarehouse(order.getFactoryId(), order.getVendorId());
            if (vendorWh != null) {
                draft.setWarehouseId(vendorWh.getWarehouseId());
                draft.setWarehouseCode(vendorWh.getWarehouseCode());
                draft.setWarehouseName(vendorWh.getWarehouseName());
            }
        }
```
类顶部 import：
```java
import com.ruoyi.system.domain.mes.wm.WmWarehouse;
```

- [ ] **Step 3: 编译 + 重启 + 实测**

```bash
cd backend && mvn -pl ruoyi-admin -am package -DskipTests -q
# 重启后：对供应商已有专属仓的采购单调 buildFromPurOrder（从采购单生成入库单）
TOKEN=$(python3 backend/scripts/get_token.py 2>/dev/null)
curl -s "http://localhost:8081/mes/wm/item-recpt/buildFromPurOrder?orderId=<供应商有专属仓的采购单ID>" -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```
Expected: 返回草稿 `warehouseId` = 供应商仓 ID。

- [ ] **Step 4: Commit**

```bash
git add backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/impl/WmItemRecptServiceImpl.java
git commit -m "feat(wm/item_recpt): 采购入库定向入供应商专属仓"
```

---

## Task 7: 销售出库客户仓硬隔离校验

**Files:**
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/impl/WmProductSalesServiceImpl.java`（方法 `doPostOut` 行 170-189，循环前）

**Interfaces:**
- Consumes: `wmWarehouseService.selectWmWarehouseByWarehouseId`（已存在）；`WmWarehouseConstants.TYPE_CUSTOMER`（Task 2）；`header.getClientId()`（已存在）

- [ ] **Step 1: 注入 IWmWarehouseService（若未注入）**

类字段区加：
```java
    @Autowired
    private com.ruoyi.system.service.mes.wm.IWmWarehouseService wmWarehouseService;
```

- [ ] **Step 2: doPostOut 循环前加校验调用**

在 `Map<Long, WmProductSalesLine> lineMap = buildLineMap(salesId);` 后、`for (WmProductSalesDetail d : details)` 前插入：
```java
        validateClientWarehouseIsolation(header, details);
```

- [ ] **Step 3: 新增私有方法**

```java
    /**
     * 客户仓硬隔离：客户专属仓(warehouse_type=CUSTOMER)的货只能发给归属客户。
     * 决策B：出库目标仓库若是客户仓，要求出库单 clientId == 仓库 clientId，否则拦截。
     * 公共成品仓(type!=CUSTOMER)不受限。
     */
    private void validateClientWarehouseIsolation(WmProductSales header, List<WmProductSalesDetail> details) {
        java.util.Set<Long> whIds = new java.util.HashSet<>();
        if (header.getWarehouseId() != null) whIds.add(header.getWarehouseId());
        if (details != null) {
            for (WmProductSalesDetail d : details) {
                if (d.getWarehouseId() != null) whIds.add(d.getWarehouseId());
            }
        }
        Long clientId = header.getClientId();
        for (Long whId : whIds) {
            WmWarehouse wh = wmWarehouseService.selectWmWarehouseByWarehouseId(whId);
            if (wh != null && WmWarehouseConstants.TYPE_CUSTOMER.equals(wh.getWarehouseType())
                    && (clientId == null || !clientId.equals(wh.getClientId()))) {
                throw new ServiceException("仓库[" + wh.getWarehouseName()
                        + "]为客户专属仓，不能给当前客户发货");
            }
        }
    }
```
类顶部 import：
```java
import com.ruoyi.common.enums.WmWarehouseConstants;
import com.ruoyi.system.domain.mes.wm.WmWarehouse;
```

- [ ] **Step 4: 编译 + 重启 + 实测拦截**

```bash
cd backend && mvn -pl ruoyi-admin -am package -DskipTests -q
# 重启后：构造从客户A仓发货给客户B的出库单，调 postOut 应被拦截
TOKEN=$(python3 backend/scripts/get_token.py 2>/dev/null)
curl -s -X POST "http://localhost:8081/mes/wm/product-sales/post" -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"salesId":<客户B出库单,仓库指向客户A仓>, "details":[...]}' | python3 -m json.tool
```
Expected: 返回 `code:500` 且 msg 含「为客户专属仓，不能给当前客户发货」。

- [ ] **Step 5: Commit**

```bash
git add backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/impl/WmProductSalesServiceImpl.java
git commit -m "feat(wm/product_sales): 销售出库客户仓硬隔离校验(防错发)"
```

---

## Task 8: 外协发料软优先供应商仓

**Files:**
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/OutsourceIssueHelper.java`（注入区行 40-54；方法 `issueOutsourceForProcess` 行 94-150；`batchFifoStocks` 行 156-184）

**Interfaces:**
- Consumes: `wmWarehouseService.findVendorWarehouse(factoryId, vendorId)`（Task 3）；`rp.getVendorId()`（已存在）；`wmMaterialStockMapper.selectByItemIds`（已存在）

- [ ] **Step 1: 注入 IWmWarehouseService**

在注入区（行 43-54）加：
```java
    @Autowired
    private com.ruoyi.system.service.mes.wm.IWmWarehouseService wmWarehouseService;
```

- [ ] **Step 2: 改造 batchFifoStocks 支持「优先指定仓库」**

把 `batchFifoStocks(List<Long> itemIds)` 改为带优先仓库参数：
```java
    /**
     * 批量 FIFO 预取：按 itemId 分组取最早可用批次。
     * @param preferWarehouseId 非空时，同物料优先取该仓库的库存；该仓无则取任意仓库最早批次。
     */
    private Map<Long, WmMaterialStock> batchFifoStocks(List<Long> itemIds, Long preferWarehouseId) {
        if (itemIds == null || itemIds.isEmpty()) {
            return new LinkedHashMap<>();
        }
        try {
            List<WmMaterialStock> all = wmMaterialStockMapper.selectByItemIds(itemIds);
            if (all == null || all.isEmpty()) {
                return new LinkedHashMap<>();
            }
            return all.stream()
                    .filter(s -> s.getQuantityAvailable() != null
                            && s.getQuantityAvailable().signum() > 0
                            && "NORMAL".equals(s.getQualityStatus()))
                    .collect(Collectors.groupingBy(
                            WmMaterialStock::getItemId,
                            Collectors.reducing((a, b) -> preferFifo(a, b, preferWarehouseId))))
                    .entrySet().stream()
                    .filter(e -> e.getValue().isPresent())
                    .collect(Collectors.toMap(
                            Map.Entry::getKey,
                            e -> e.getValue().get(),
                            (a, b) -> a,
                            LinkedHashMap::new));
        } catch (Exception e) {
            log.debug("外协草稿批量 FIFO 预取跳过, itemIds={}", itemIds, e);
            return new LinkedHashMap<>();
        }
    }

    /** FIFO 归约：优先 preferWarehouseId 的记录；同仓或都不在时取 createTime 最早。 */
    private WmMaterialStock preferFifo(WmMaterialStock a, WmMaterialStock b, Long preferWarehouseId) {
        if (preferWarehouseId != null) {
            boolean aPref = preferWarehouseId.equals(a.getWarehouseId());
            boolean bPref = preferWarehouseId.equals(b.getWarehouseId());
            if (aPref && !bPref) return a;
            if (!aPref && bPref) return b;
        }
        Date ta = a.getCreateTime() != null ? a.getCreateTime() : new Date(0);
        Date tb = b.getCreateTime() != null ? b.getCreateTime() : new Date(0);
        return ta.compareTo(tb) <= 0 ? a : b;
    }
```
确认 import：`java.util.Date`、`java.util.LinkedHashMap`（已存在）。

- [ ] **Step 3: issueOutsourceForProcess 传入供应商仓优先**

定位行 107-109：
```java
    Map<Long, WmMaterialStock> fifoByItem = batchFifoStocks(bomItemIds);
```
替换为：
```java
    // 软优先供应商仓：外协供应商有专属仓时，发料行优先从该供应商仓取库存（不足自动回落公共仓）
    Long vendorWarehouseId = null;
    if (rp.getVendorId() != null) {
        WmWarehouse vendorWh = wmWarehouseService.findVendorWarehouse(wo.getFactoryId(), rp.getVendorId());
        vendorWarehouseId = vendorWh != null ? vendorWh.getWarehouseId() : null;
    }
    Map<Long, WmMaterialStock> fifoByItem = batchFifoStocks(bomItemIds, vendorWarehouseId);
```
类顶部 import：
```java
import com.ruoyi.system.domain.mes.wm.WmWarehouse;
```

- [ ] **Step 4: 编译 + 重启 + 实测**

```bash
cd backend && mvn -pl ruoyi-admin -am package -DskipTests -q
# 重启后：对供应商有专属仓且仓内有料的外协工序触发生成外协发料单草稿
# 检查发料行 warehouseId 是否优先落在供应商仓
```
Expected: 供应商仓有库存时，发料行 `warehouseId` = 供应商仓；供应商仓无库存时回落公共仓（现状行为不变）。

- [ ] **Step 5: Commit**

```bash
git add backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/wm/OutsourceIssueHelper.java
git commit -m "feat(wm/outsource): 外协发料软优先从供应商专属仓发料"
```

---

## Task 9: 前端 — 仓库管理页支持客户/供应商仓

**Files:**
- Modify: `frontend/src/api/mes/wm/warehouse.ts`
- Modify: `frontend/src/views/mes/wm/warehouse/index.vue`

**Interfaces:**
- Consumes: 后端仓库 CRUD（已支持新字段）

- [ ] **Step 1: warehouse.ts 类型加 clientId/vendorId**

在 Warehouse 类型加（**不硬编码类型枚举**——前端下拉走现有字典 `mes_warehouse_type`）：
```ts
  clientId?: number
  vendorId?: number
```

- [ ] **Step 2: index.vue 表单加归属选择器（类型下拉已用字典，无需改）**

`warehouse/index.vue` 已有 `const { mes_warehouse_type } = useDict('mes_warehouse_type')`（行 141），并已用于类型下拉（行 81-83）与 `dict-tag` 展示（行 36）。Task 1 已把 CUSTOMER/SUPPLIER 写入字典，前端自动出现新选项。本步只加归属：
- 表单（el-form）按 `form.warehouseType` 显隐：
  - `=== 'CUSTOMER'` → 显示「归属客户」用 `ClientSelect` 组件，绑定 `form.clientId`
  - `=== 'SUPPLIER'` → 显示「归属供应商」用 `VendorSelect` 组件，绑定 `form.vendorId`
- `address` 输入框（已有则复用，作收发货地址）
- 列表表格加「归属」列（展示 clientName/vendorName）
- 提交前（submitForm）按类型清空无关归属字段（client/supplier 互斥），与后端 `normalizeWarehouseOwner` 一致

- [ ] **Step 3: 浏览器实测**

```bash
cd frontend && npm run dev   # 或 dev-start skill
```
打开仓库管理页 → 新增 → 类型下拉应已含「客户仓/供应商仓」（来自字典）→ 选「客户仓」→ 出现归属客户选择 → 填名称+地址+客户 → 保存成功；不填客户提交应被后端拦截报错。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/api/mes/wm/warehouse.ts frontend/src/views/mes/wm/warehouse/index.vue
git commit -m "feat(fe/wm): 仓库管理页支持客户/供应商仓类型与归属"
```

---

## Task 10: 前端 — WarehouseSelect 即时新建客户/供应商仓

**Files:**
- Modify: `frontend/src/components/warehouseSelect/single.vue`
- Create: `frontend/src/components/warehouseSelect/CreateWarehouseDialog.vue`（若 single.vue 超 300 行则拆出）

**Interfaces:**
- Consumes: `addWmWarehouse` API；`ClientSelect`/`VendorSelect`

- [ ] **Step 1: 新建 CreateWarehouseDialog.vue**

紧凑弹窗：仓库名称(必填)、地址(必填)、类型(CUSTOMER/SUPPLIER 二选一)、归属客户/供应商（按类型显隐）、仓库编码(可选，空则后端规则生成)。
提交调 `addWmWarehouse`，成功后 emit 新建的 warehouse 对象。

- [ ] **Step 2: single.vue 加「➕ 新建客户仓/供应商仓」按钮**

在仓库选择弹窗/工具栏加按钮，点击打开 CreateWarehouseDialog；建完自动选中新建仓并回填到当前单据字段。

- [ ] **Step 3: 浏览器实测**

在产品入库/销售出库页面选仓库时 → 点「新建」→ 建客户仓 → 自动选中填入。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/warehouseSelect/
git commit -m "feat(fe/wm): 仓库选择器支持即时新建客户/供应商仓"
```

---

## Task 11: 前端 — 产品入库单展示客户

**Files:**
- Modify: `frontend/src/views/mes/wm/productrecpt/index.vue`

- [ ] **Step 1: 列表/详情加客户列**

表格加「客户」列（展示 clientName——若列表 VO 未带 clientName，后端 WmProductRecptMapper 的 list 查询 JOIN qxx_md_client 取 client_name，或前端按 clientId 查；优先后端 JOIN）。表单详情展示客户（只读，来源工单）。

- [ ] **Step 2: 浏览器实测**

带客户工单生成的入库单 → 列表/详情正确显示客户。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/views/mes/wm/productrecpt/index.vue
git commit -m "feat(fe/wm): 产品入库单展示归属客户"
```

---

## Task 12: 前端 — 销售出库发货仓库过滤

**Files:**
- Modify: `frontend/src/views/mes/wm/product_sales/components/SalesOutDialog.vue`（过账出库弹窗）及相关仓库选择处

- [ ] **Step 1: 出库仓库下拉按客户过滤**

出库单有 clientId 时，发货仓库候选 = 该客户专属仓 + 公共成品仓（客户仓置顶）。无 clientId 时显示全部。过滤可在 WarehouseSelect 传参（clientId）或前端拉取后本地过滤。

- [ ] **Step 2: 浏览器实测**

选客户A的出库单 → 发货仓库下拉优先显示客户A仓 + 公共成品仓。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/views/mes/wm/product_sales/
git commit -m "feat(fe/wm): 销售出库发货仓库按客户过滤(客户仓置顶)"
```

---

## Task 13: 全链路验证

- [ ] **Step 1: 后端打包重启**

```bash
cd backend && mvn -pl ruoyi-admin -am package -DskipTests -q
# kill 旧 jar 进程，重启 ruoyi-admin.jar，等 captchaImage 200
```

- [ ] **Step 2: 客户线端到端**
1. 仓库管理新建客户仓（客户A，带地址）
2. 销售订单转工单（带客户A）→ 报工产出 → 自动生成入库单，确认入库定向落客户A仓、client_id=A
3. 销售出库（客户A）从客户A仓发货 → postOut 成功
4. 销售出库（客户B）试图从客户A仓发货 → 被拦截
5. 备货工单（无客户）入库 → 落公共成品仓（不受影响）

- [ ] **Step 3: 供应商线端到端**
1. 新建供应商仓（供应商X，带地址）
2. 采购入库（供应商X）→ buildFromPurOrder 草稿 warehouseId 预填供应商X仓
3. 外协发料（供应商X，仓内有料）→ 发料行优先落供应商X仓；仓内无料回落公共原料仓

- [ ] **Step 4: 即时新建仓**
产品入库/销售出库选仓库时即时新建客户/供应商仓并选中。

- [ ] **Step 5: 验证通过后**

无新增提交（本任务为验证）。若发现问题回到对应 Task 修复。
