# 二维码 P1：流转卡码 + 扫码报工 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让工人 App 扫流转卡二维码即可报工（4 步压成 2 步），并在 PC 流转卡管理页生成/展示/打印流转卡二维码。

**Architecture:** 二维码内容用带前缀的字符串 `QXX|CARD|{cardCode}`，前端 canvas 实时渲染、后端不存图片。新增后端 `GET /mes/pro/procard/scan/{cardCode}` 一个接口返回报工所需全部上下文（卡/工单/当前工序/可报任务/消耗默认值/已报合格数），App 扫码后直接进填数量页。

**Tech Stack:** 后端 Spring Boot 4.0.3 / JDK 17 / MyBatis（JUnit5 + Mockito + AssertJ 单测，无需 Docker/Redis）；前端 Vue 3.5 + TS + Element Plus + `qrcode` npm；App uni-app + Vue 3（`uni.scanCode` 扫码，P1 不需 QR 渲染库）。

## Global Constraints

- **只改本项目文件**（`backend/`、`frontend/`、`app/`），workspace 其他目录只读。
- **所有 SQL WHERE 带 `factory_id`**（MyBatis 拦截器自动注入参数值，SQL 仍写 `<if>`）。`selectProCardList` 已走拦截器，按 `card_code` 查自动带 factory 作用域。
- **后端改动验证红线**：改了 Java 必须 `mvn -pl ruoyi-admin -am package -DskipTests` 重新打包 → 重启 jar → 用 token 实测接口（见各任务）。`mvn compile` 通过 ≠ 修复完成。
- **前端改动**：必须在浏览器实际点一次确认；**App 改动**：必须在真机/模拟器点一次。
- **API 契约**：单条 `{code:200,msg,data:{...}}`；后端新增接口须同步前端 API 模块，字段 camelCase。
- **Commit**：每完成一个 Task 提交一次（用户已授权"开始实现"）。当前分支 `feature/slitting-outsource`；建议本计划工作切到新分支 `feature/qr-code-p1`（执行时第一步）。
- **设计依据**：`docs/superpowers/specs/2026-08-13-二维码扫码体系-design.md`（§3 码体系、§4 生成时机、§5 扫码矩阵、§6.2 scanForReport 接口）。

---

## File Structure

| 文件 | 责任 | 动作 |
|------|------|------|
| `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayloadType.java` | 码类型枚举 | 新建 |
| `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayload.java` | 解析结果持有者 | 新建 |
| `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayloadUtil.java` | payload 构造/解析工具 | 新建 |
| `backend/ruoyi-common/pom.xml` | 加 test 依赖 | 修改 |
| `backend/ruoyi-common/src/test/java/com/ruoyi/common/utils/qr/QrPayloadUtilTest.java` | 工具单测 | 新建 |
| `backend/ruoyi-system/src/main/java/com/ruoyi/system/domain/mes/pro/CardScanResultVO.java` | 扫码反查返回 VO | 新建 |
| `backend/ruoyi-system/.../service/mes/pro/IProCardService.java` | 加 `scanForReport` | 修改 |
| `backend/ruoyi-system/.../service/mes/pro/impl/ProCardServiceImpl.java` | 实现 scanForReport + 注入 3 个依赖 | 修改 |
| `backend/ruoyi-admin/.../controller/mes/pro/ProCardController.java` | 加 `/scan/{cardCode}` | 修改 |
| `backend/ruoyi-system/src/test/.../ProCardScanServiceUnitTest.java` | scanForReport 单测 | 新建 |
| `frontend/src/utils/qrPayload.ts` | 前端 payload 构造/解析 | 新建 |
| `frontend/src/components/QrCode/index.vue` | canvas 渲染二维码组件 | 新建 |
| `frontend/src/views/mes/pro/card/index.vue` | 二维码弹窗 + 批量打印 | 修改 |
| `app/utils/qrPayload.js` | App payload 构造/解析 | 新建 |
| `app/utils/scanDispatch.js` | 统一扫码 + 前缀分发 | 新建 |
| `app/api/mes/pro/procard.js` | `getCardScanResult` | 新建 |
| `app/pages/work/index.vue` | 工作台加「扫一扫」入口 | 修改 |
| `app/pages/mes/pro/report.vue` | onLoad 接 cardCode → 扫码报工 2 步流 | 修改 |

---

## Task 1: 后端 QrPayloadUtil 工具（TDD）

**Files:**
- Create: `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayloadType.java`
- Create: `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayload.java`
- Create: `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayloadUtil.java`
- Modify: `backend/ruoyi-common/pom.xml`
- Test: `backend/ruoyi-common/src/test/java/com/ruoyi/common/utils/qr/QrPayloadUtilTest.java`

**Interfaces:**
- Produces: `QrPayloadUtil.build(QrPayloadType, String) -> String`、`QrPayloadUtil.parse(String) -> QrPayload|null`、`QrPayload{getType(),getCode()}`、`QrPayloadType{CARD,MAT,ROLL,WO,PKG; from(String)}`

- [ ] **Step 1: 给 ruoyi-common 加 test 依赖**

在 `backend/ruoyi-common/pom.xml` 的 `<dependencies>` 内加入（版本由父 POM 的 spring-boot BOM 管理，无需写 version）：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
</dependency>
```

- [ ] **Step 2: 先写失败的单测**

Create `backend/ruoyi-common/src/test/java/com/ruoyi/common/utils/qr/QrPayloadUtilTest.java`：

```java
package com.ruoyi.common.utils.qr;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.*;

class QrPayloadUtilTest {

    @Test
    void build_card() {
        assertThat(QrPayloadUtil.build(QrPayloadType.CARD, "CRD20260620001"))
                .isEqualTo("QXX|CARD|CRD20260620001");
    }

    @Test
    void parse_card() {
        QrPayload p = QrPayloadUtil.parse("QXX|CARD|CRD20260620001");
        assertThat(p).isNotNull();
        assertThat(p.getType()).isEqualTo(QrPayloadType.CARD);
        assertThat(p.getCode()).isEqualTo("CRD20260620001");
    }

    @Test
    void parse_nonQxx_or_bareCode_returnsNull() {
        assertThat(QrPayloadUtil.parse("https://example.com")).isNull();
        assertThat(QrPayloadUtil.parse("CRD20260620001")).isNull(); // 裸卡号
        assertThat(QrPayloadUtil.parse(null)).isNull();
    }

    @Test
    void parse_unknownType_returnsNull() {
        assertThat(QrPayloadUtil.parse("QXX|FOO|bar")).isNull();
    }

    @Test
    void parse_emptyCode_returnsNull() {
        assertThat(QrPayloadUtil.parse("QXX|CARD|")).isNull();
    }

    @Test
    void build_nullArgs_throws() {
        assertThatThrownBy(() -> QrPayloadUtil.build(null, "x"))
                .isInstanceOf(IllegalArgumentException.class);
        assertThatThrownBy(() -> QrPayloadUtil.build(QrPayloadType.CARD, ""))
                .isInstanceOf(IllegalArgumentException.class);
    }

    @Test
    void roundtrip_allTypes() {
        for (QrPayloadType t : QrPayloadType.values()) {
            QrPayload p = QrPayloadUtil.parse(QrPayloadUtil.build(t, "CODE123"));
            assertThat(p).isNotNull();
            assertThat(p.getType()).isEqualTo(t);
            assertThat(p.getCode()).isEqualTo("CODE123");
        }
    }
}
```

- [ ] **Step 3: 运行单测确认失败（类不存在）**

Run: `cd backend && mvn -pl ruoyi-common -am test -Dtest=QrPayloadUtilTest`
Expected: 编译失败 / ClassNotFoundException（工具类尚未创建）。

- [ ] **Step 4: 实现 QrPayloadType**

Create `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayloadType.java`：

```java
package com.ruoyi.common.utils.qr;

/**
 * 二维码 payload 类型前缀（见设计文档 §3.1）。
 */
public enum QrPayloadType {
    CARD, MAT, ROLL, WO, PKG;

    public static QrPayloadType from(String name) {
        if (name == null) {
            return null;
        }
        try {
            return QrPayloadType.valueOf(name);
        } catch (IllegalArgumentException e) {
            return null;
        }
    }
}
```

- [ ] **Step 5: 实现 QrPayload**

Create `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayload.java`：

```java
package com.ruoyi.common.utils.qr;

/**
 * 二维码 payload 解析结果。
 */
public class QrPayload {

    private final QrPayloadType type;
    private final String code;

    public QrPayload(QrPayloadType type, String code) {
        this.type = type;
        this.code = code;
    }

    public QrPayloadType getType() {
        return type;
    }

    public String getCode() {
        return code;
    }
}
```

- [ ] **Step 6: 实现 QrPayloadUtil**

Create `backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/QrPayloadUtil.java`：

```java
package com.ruoyi.common.utils.qr;

/**
 * 二维码 payload 构造与解析工具。格式：QXX|TYPE|CODE
 * 设计文档 §3.1。
 */
public final class QrPayloadUtil {

    private static final String PREFIX = "QXX";
    private static final String SEP = "\\|";

    private QrPayloadUtil() {
    }

    public static String build(QrPayloadType type, String code) {
        if (type == null || code == null || code.isEmpty()) {
            throw new IllegalArgumentException("type 与 code 不能为空");
        }
        return PREFIX + "|" + type.name() + "|" + code;
    }

    public static QrPayload parse(String raw) {
        if (raw == null) {
            return null;
        }
        String[] parts = raw.split(SEP, 3);
        if (parts.length < 3 || !PREFIX.equals(parts[0])) {
            return null;
        }
        QrPayloadType type = QrPayloadType.from(parts[1]);
        if (type == null) {
            return null;
        }
        String code = parts[2];
        if (code == null || code.isEmpty()) {
            return null;
        }
        return new QrPayload(type, code);
    }
}
```

- [ ] **Step 7: 运行单测确认通过**

Run: `cd backend && mvn -pl ruoyi-common -am test -Dtest=QrPayloadUtilTest`
Expected: PASS（7 个测试全绿，无需 Docker/Redis）。

- [ ] **Step 8: Commit**

```bash
git add backend/ruoyi-common/src/main/java/com/ruoyi/common/utils/qr/ \
        backend/ruoyi-common/src/test/java/com/ruoyi/common/utils/qr/ \
        backend/ruoyi-common/pom.xml
git commit -m "feat(common): 二维码 payload 工具 QrPayloadUtil + 单测"
```

---

## Task 2: 后端 scanForReport 扫码反查接口（TDD）

**Files:**
- Create: `backend/ruoyi-system/src/main/java/com/ruoyi/system/domain/mes/pro/CardScanResultVO.java`
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/pro/IProCardService.java`
- Modify: `backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/pro/impl/ProCardServiceImpl.java`
- Modify: `backend/ruoyi-admin/src/main/java/com/ruoyi/web/controller/mes/pro/ProCardController.java`
- Test: `backend/ruoyi-system/src/test/java/com/ruoyi/system/service/mes/pro/ProCardScanServiceUnitTest.java`

**Interfaces:**
- Consumes: `ProCardMapper.selectProCardList(ProCard)`（按 cardCode 查，已存在，走 factory_id 拦截器）；`ProTaskMapper.selectProTaskList(ProTask)`；`ProFeedbackMapper.selectPendingTaskIds(Collection<Long>)`、`sumAuditedQualifiedByCardAndProcess(Long,Long)`；`IProFeedbackService.getDefaultConsume(Long)`。常量 `ProConstants.WS_CODE_VENDOR`、`ProConstants.TASK_STATUS_PRODUCING`。
- Produces: `IProCardService.scanForReport(String cardCode) -> CardScanResultVO`；REST `GET /mes/pro/procard/scan/{cardCode}`。

> **依赖注入说明（避免循环）**：ProCardServiceImpl 新增注入 `ProTaskMapper`、`ProFeedbackMapper`、`IProFeedbackService`。这三个**不会**与 ProCardServiceImpl 循环——ProFeedbackServiceImpl 只注入 `ProCardMapper`（mapper，非 service），mapper 之间不循环。

- [ ] **Step 1: 创建 CardScanResultVO**

Create `backend/ruoyi-system/src/main/java/com/ruoyi/system/domain/mes/pro/CardScanResultVO.java`：

```java
package com.ruoyi.system.domain.mes.pro;

import java.math.BigDecimal;
import java.util.List;

/**
 * 扫流转卡码反查报工上下文（设计文档 §6.2）。
 */
public class CardScanResultVO {

    /** 流转卡（携带 cardCode/status/currentProcess/workorder/item 等冗余字段） */
    private ProCard card;

    /** 当前可报任务（工单下、当前工序、PRODUCING、非外协工位） */
    private List<ProTask> reportableTasks;

    /** BOM 消耗默认值 */
    private List<ProFeedbackConsume> consumeDefaults;

    /** 该卡该工序已审核合格数（防超报） */
    private BigDecimal reportedQualifiedSum;

    /** 是否可报工 */
    private boolean canReport;

    /** 不可报原因码：CARD_NOT_FOUND / CARD_COMPLETED / CARD_OUTSOURCING / CARD_SCRAPPED / NO_REPORTABLE_TASK */
    private String reason;

    public ProCard getCard() { return card; }
    public void setCard(ProCard card) { this.card = card; }

    public List<ProTask> getReportableTasks() { return reportableTasks; }
    public void setReportableTasks(List<ProTask> reportableTasks) { this.reportableTasks = reportableTasks; }

    public List<ProFeedbackConsume> getConsumeDefaults() { return consumeDefaults; }
    public void setConsumeDefaults(List<ProFeedbackConsume> consumeDefaults) { this.consumeDefaults = consumeDefaults; }

    public BigDecimal getReportedQualifiedSum() { return reportedQualifiedSum; }
    public void setReportedQualifiedSum(BigDecimal reportedQualifiedSum) { this.reportedQualifiedSum = reportedQualifiedSum; }

    public boolean isCanReport() { return canReport; }
    public void setCanReport(boolean canReport) { this.canReport = canReport; }

    public String getReason() { return reason; }
    public void setReason(String reason) { this.reason = reason; }
}
```

- [ ] **Step 2: IProCardService 加方法签名**

在 `IProCardService.java` 接口体内（现有方法之后）加：

```java
    /** 扫流转卡码反查报工上下文 */
    com.ruoyi.system.domain.mes.pro.CardScanResultVO scanForReport(String cardCode);
```

（若文件已 import `CardScanResultVO` 同包则无需全限定名；二者同包 `com.ruoyi.system.domain.mes.pro`，但接口在 `service.mes.pro` 包，故用全限定名或加 import。建议加 `import com.ruoyi.system.domain.mes.pro.CardScanResultVO;` 然后写 `CardScanResultVO scanForReport(String cardCode);`。）

- [ ] **Step 3: ProCardServiceImpl 注入依赖 + 实现**

先 **Read** `ProCardServiceImpl.java` 确认现有注入字段写法（`@Autowired` 还是构造器），按相同风格添加。

在类字段区加（用与现有字段一致的注入注解，通常 `@Autowired`）：

```java
    @Autowired
    private com.ruoyi.system.mapper.mes.pro.ProTaskMapper proTaskMapper;

    @Autowired
    private com.ruoyi.system.mapper.mes.pro.ProFeedbackMapper proFeedbackMapper;

    @Autowired
    private com.ruoyi.system.service.mes.pro.IProFeedbackService proFeedbackService;
```

在 import 区加：

```java
import com.ruoyi.system.domain.mes.pro.CardScanResultVO;
import com.ruoyi.system.domain.mes.pro.ProConstants;
import com.ruoyi.system.domain.mes.pro.ProFeedbackConsume;
import com.ruoyi.system.domain.mes.pro.ProTask;
import com.ruoyi.system.mapper.mes.pro.ProFeedbackMapper;
import com.ruoyi.system.mapper.mes.pro.ProTaskMapper;
import java.math.BigDecimal;
import java.util.Collections;
import java.util.HashSet;
import java.util.Set;
import java.util.stream.Collectors;
```

在类内加实现方法（≤50 行，符合代码质量约束）：

```java
    @Override
    public CardScanResultVO scanForReport(String cardCode) {
        CardScanResultVO vo = new CardScanResultVO();
        // 1. 按 cardCode 查卡（factory_id 由拦截器自动注入）
        ProCard q = new ProCard();
        q.setCardCode(cardCode);
        List<ProCard> list = proCardMapper.selectProCardList(q);
        if (list == null || list.isEmpty()) {
            vo.setCanReport(false);
            vo.setReason("CARD_NOT_FOUND");
            return vo;
        }
        ProCard card = list.get(0);
        vo.setCard(card);

        // 2. 卡状态校验
        if (!"ACTIVE".equals(card.getStatus())) {
            vo.setCanReport(false);
            vo.setReason(reasonForCardStatus(card.getStatus()));
            return vo;
        }

        // 3. 加载工单任务，筛可报（PRODUCING + 非外协工位）
        ProTask tq = new ProTask();
        tq.setWorkorderId(card.getWorkorderId());
        List<ProTask> all = proTaskMapper.selectProTaskList(tq);
        List<ProTask> reportable = all.stream()
                .filter(t -> ProConstants.TASK_STATUS_PRODUCING.equals(t.getStatus()))
                .filter(t -> !ProConstants.WS_CODE_VENDOR.equals(t.getWorkstationCode()))
                .collect(Collectors.toList());
        fillPendingCount(reportable);
        vo.setReportableTasks(reportable);

        // 4. BOM 消耗默认值（失败不阻断）
        try {
            vo.setConsumeDefaults(proFeedbackService.getDefaultConsume(card.getWorkorderId()));
        } catch (Exception e) {
            vo.setConsumeDefaults(Collections.emptyList());
        }

        // 5. 已审核合格数（防超报）
        if (card.getCurrentProcessId() != null) {
            vo.setReportedQualifiedSum(
                    proFeedbackMapper.sumAuditedQualifiedByCardAndProcess(card.getCardId(), card.getCurrentProcessId()));
        } else {
            vo.setReportedQualifiedSum(BigDecimal.ZERO);
        }

        // 6. 可报判定
        if (reportable.isEmpty()) {
            vo.setCanReport(false);
            vo.setReason("NO_REPORTABLE_TASK");
        } else {
            vo.setCanReport(true);
        }
        return vo;
    }

    private String reasonForCardStatus(String status) {
        if (status == null) {
            return "CARD_NOT_ACTIVE";
        }
        switch (status) {
            case "COMPLETED": return "CARD_COMPLETED";
            case "OUTSOURCING": return "CARD_OUTSOURCING";
            case "SCRAPPED": return "CARD_SCRAPPED";
            default: return "CARD_NOT_ACTIVE";
        }
    }

    private void fillPendingCount(List<ProTask> tasks) {
        if (tasks == null || tasks.isEmpty()) {
            return;
        }
        List<Long> ids = tasks.stream().map(ProTask::getTaskId).collect(Collectors.toList());
        List<Long> pending = proFeedbackMapper.selectPendingTaskIds(ids);
        Set<Long> pendingSet = (pending == null) ? Collections.emptySet() : new HashSet<>(pending);
        for (ProTask t : tasks) {
            t.setPendingFeedbackCount(pendingSet.contains(t.getTaskId()) ? 1 : 0);
        }
    }
```

> **验证点（实现前 Read 确认）**：① `ProTaskMapper.selectProTaskList(ProTask)` 方法名存在；② `ProTask.getPendingFeedbackCount()/setPendingFeedbackCount(int)` 字段类型；③ `ProConstants.TASK_STATUS_PRODUCING` 与 `WS_CODE_VENDOR` 常量名。若任一名不一致，按实际名调整。

- [ ] **Step 4: Controller 加端点**

在 `ProCardController.java` 加（紧跟现有 `getInfo` 端点之后；import `CardScanResultVO` 同包 `domain.mes.pro`，controller 在 `web.controller.mes.pro`，需 import）：

```java
    /**
     * 扫流转卡码反查报工上下文（App 扫码报工入口）
     */
    @PreAuthorize("@ss.hasPermi('mes:pro:card:query')")
    @GetMapping("/scan/{cardCode}")
    public AjaxResult scanForReport(@PathVariable("cardCode") String cardCode) {
        return success(proCardService.scanForReport(cardCode));
    }
```

- [ ] **Step 5: 先写失败的单测**

Create `backend/ruoyi-system/src/test/java/com/ruoyi/system/service/mes/pro/ProCardScanServiceUnitTest.java`（仿 `ProFeedbackServiceUnitTest` 的 Mockito 风格）：

```java
package com.ruoyi.system.service.mes.pro;

import com.ruoyi.system.domain.mes.pro.CardScanResultVO;
import com.ruoyi.system.domain.mes.pro.ProCard;
import com.ruoyi.system.domain.mes.pro.ProTask;
import com.ruoyi.system.mapper.mes.pro.ProCardMapper;
import com.ruoyi.system.mapper.mes.pro.ProFeedbackMapper;
import com.ruoyi.system.mapper.mes.pro.ProTaskMapper;
import com.ruoyi.system.service.mes.pro.impl.ProCardServiceImpl;
import com.ruoyi.system.service.mes.sys.generator.AutoCodeGenerator;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.transaction.PlatformTransactionManager;

import java.math.BigDecimal;
import java.util.Collections;
import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyLong;
import static org.mockito.Mockito.when;

@ExtendWith(MockitoExtension.class)
class ProCardScanServiceUnitTest {

    @Mock private ProCardMapper proCardMapper;
    @Mock private ProTaskMapper proTaskMapper;
    @Mock private ProFeedbackMapper proFeedbackMapper;
    @Mock private IProFeedbackService proFeedbackService;
    @Mock private RedisTemplate<String, Object> redisTemplate;
    @Mock private PlatformTransactionManager platformTransactionManager;
    @Mock private AutoCodeGenerator autoCodeGenerator;

    @InjectMocks private ProCardServiceImpl service;

    @Test
    void cardNotFound() {
        when(proCardMapper.selectProCardList(any())).thenReturn(Collections.emptyList());
        CardScanResultVO vo = service.scanForReport("NOPE");
        assertThat(vo.isCanReport()).isFalse();
        assertThat(vo.getReason()).isEqualTo("CARD_NOT_FOUND");
    }

    @Test
    void cardCompleted_cannotReport() {
        ProCard c = new ProCard();
        c.setCardCode("CRD1");
        c.setStatus("COMPLETED");
        when(proCardMapper.selectProCardList(any())).thenReturn(List.of(c));
        CardScanResultVO vo = service.scanForReport("CRD1");
        assertThat(vo.isCanReport()).isFalse();
        assertThat(vo.getReason()).isEqualTo("CARD_COMPLETED");
    }

    @Test
    void activeCard_withReportableTask_canReport() {
        ProCard c = new ProCard();
        c.setCardId(1L);
        c.setCardCode("CRD1");
        c.setStatus("ACTIVE");
        c.setWorkorderId(10L);
        c.setCurrentProcessId(5L);
        when(proCardMapper.selectProCardList(any())).thenReturn(List.of(c));

        ProTask t = new ProTask();
        t.setTaskId(33L);
        t.setStatus("PRODUCING");
        t.setWorkstationCode("WS1");
        when(proTaskMapper.selectProTaskList(any())).thenReturn(List.of(t));
        when(proFeedbackMapper.selectPendingTaskIds(any())).thenReturn(Collections.emptyList());
        when(proFeedbackService.getDefaultConsume(10L)).thenReturn(Collections.emptyList());
        when(proFeedbackMapper.sumAuditedQualifiedByCardAndProcess(1L, 5L)).thenReturn(BigDecimal.TEN);

        CardScanResultVO vo = service.scanForReport("CRD1");
        assertThat(vo.isCanReport()).isTrue();
        assertThat(vo.getReportableTasks()).hasSize(1);
        assertThat(vo.getReportedQualifiedSum()).isEqualByComparingTo("10");
    }

    @Test
    void activeCard_noReportableTask() {
        ProCard c = new ProCard();
        c.setCardId(1L);
        c.setCardCode("CRD1");
        c.setStatus("ACTIVE");
        c.setWorkorderId(10L);
        when(proCardMapper.selectProCardList(any())).thenReturn(List.of(c));
        when(proTaskMapper.selectProTaskList(any())).thenReturn(Collections.emptyList());

        CardScanResultVO vo = service.scanForReport("CRD1");
        assertThat(vo.isCanReport()).isFalse();
        assertThat(vo.getReason()).isEqualTo("NO_REPORTABLE_TASK");
    }
}
```

> **注**：`@InjectMocks` 需覆盖 ProCardServiceImpl 全部构造/字段依赖。若该类还有其它必填字段导致注入失败，补对应 `@Mock`（先 Read 类头确认字段列表）。`RedisTemplate` 字段名须与类内一致——若类里用的是 `RedisLockTemplate` 而非 `RedisTemplate`，改成 `@Mock RedisLockTemplate`（Read 确认）。

- [ ] **Step 6: 运行单测确认通过**

Run: `cd backend && mvn -pl ruoyi-system -am test -Dtest=ProCardScanServiceUnitTest`
Expected: PASS（4 个测试全绿）。失败则按报错调整 mock 字段/方法名。

- [ ] **Step 7: 重新打包 + 重启 + 实测接口（验证红线）**

```bash
# 重新打包
cd backend && mvn -pl ruoyi-admin -am package -DskipTests
# 重启（替换 <旧pid>）
kill <旧pid>
nohup java -jar ruoyi-admin/target/ruoyi-admin.jar > /tmp/ruoyi-backend.log 2>&1 &
# 等启动
curl -s http://localhost:8081/captchaImage -o /dev/null -w "%{http_code}\n"   # 期望 200
```

实测（取一个真实 cardCode，先查库拿到一个 ACTIVE 卡号）：

```bash
TOKEN=$(python3 backend/scripts/get_token.py 2>/dev/null)
curl -s "http://localhost:8081/mes/pro/procard/scan/CRD20260620001" \
  -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

Expected: `{ "code":200, "data":{ "card":{...}, "reportableTasks":[...], "canReport":true/false, ... } }`。验证：① 不存在的卡号返回 `CARD_NOT_FOUND`；② 已完工卡返回 `CARD_COMPLETED`。

- [ ] **Step 8: Commit**

```bash
git add backend/ruoyi-system/src/main/java/com/ruoyi/system/domain/mes/pro/CardScanResultVO.java \
        backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/pro/IProCardService.java \
        backend/ruoyi-system/src/main/java/com/ruoyi/system/service/mes/pro/impl/ProCardServiceImpl.java \
        backend/ruoyi-admin/src/main/java/com/ruoyi/web/controller/mes/pro/ProCardController.java \
        backend/ruoyi-system/src/test/java/com/ruoyi/system/service/mes/pro/ProCardScanServiceUnitTest.java
git commit -m "feat(pro): 扫流转卡码反查报工上下文接口 GET /mes/pro/procard/scan/{cardCode} + 单测"
```

---

## Task 3: 前端 qrcode 依赖 + qrPayload 工具 + QrCode 组件

**Files:**
- Modify: `frontend/package.json`（`npm install qrcode @types/qrcode`）
- Create: `frontend/src/utils/qrPayload.ts`
- Create: `frontend/src/components/QrCode/index.vue`

**Interfaces:**
- Produces: `buildQrPayload(type, code)`、`parseQrPayload(raw)`、`<QrCode :payload :size />`（按页 import）。

- [ ] **Step 1: 安装依赖**

Run: `cd frontend && npm install qrcode @types/qrcode`
Expected: `package.json` 出现 `qrcode` 与 `@types/qrcode`。

- [ ] **Step 2: 创建 qrPayload.ts**

Create `frontend/src/utils/qrPayload.ts`：

```ts
const PREFIX = 'QXX'
export type QrType = 'CARD' | 'MAT' | 'ROLL' | 'WO' | 'PKG'
const ALL_TYPES: QrType[] = ['CARD', 'MAT', 'ROLL', 'WO', 'PKG']

export function buildQrPayload(type: QrType, code: string): string {
  return `${PREFIX}|${type}|${code}`
}

export function parseQrPayload(raw: string): { type: QrType; code: string } | null {
  if (!raw) return null
  const parts = raw.split('|', 3)
  if (parts.length < 3 || parts[0] !== PREFIX) return null
  if (!ALL_TYPES.includes(parts[1] as QrType)) return null
  if (!parts[2]) return null
  return { type: parts[1] as QrType, code: parts[2] }
}

export const buildCardPayload = (cardCode: string) => buildQrPayload('CARD', cardCode)
```

- [ ] **Step 3: 创建 QrCode 组件**

Create `frontend/src/components/QrCode/index.vue`：

```vue
<template>
  <canvas ref="canvasRef"></canvas>
</template>

<script setup lang="ts">
import { ref, watch, onMounted } from 'vue'
import QRCode from 'qrcode'

const props = defineProps<{ payload: string; size?: number }>()
const canvasRef = ref<HTMLCanvasElement>()

async function render() {
  if (!canvasRef.value || !props.payload) return
  await QRCode.toCanvas(canvasRef.value, props.payload, { width: props.size ?? 200, margin: 1 })
}

onMounted(render)
watch(() => props.payload, render)
</script>
```

- [ ] **Step 4: 浏览器冒烟验证**

启动前端 dev（若未启动）：`cd frontend && npm run dev`。在任一页面临时 import `<QrCode payload="QXX|CARD|TEST" />` 看是否渲染二维码图（验证后删掉临时引用）。Expected: canvas 上出现二维码。

- [ ] **Step 5: Commit**

```bash
git add frontend/package.json frontend/package-lock.json \
        frontend/src/utils/qrPayload.ts frontend/src/components/QrCode/index.vue
git commit -m "feat(frontend): qrcode 依赖 + qrPayload 工具 + QrCode 组件"
```

---

## Task 4: 前端流转卡管理页 二维码弹窗 + 批量打印

**Files:**
- Modify: `frontend/src/views/mes/pro/card/index.vue`

**Interfaces:**
- Consumes: Task 3 的 `QrCode` 组件、`buildCardPayload`、`qrcode`（`QRCode.toDataURL`）。

> 动手前 **Read** `card/index.vue` 全文（516 行）确认：行操作列（~84-91）、工具栏（~44-59）、`dataList`/`ids`/`single`/`multiple` refs、`<script setup>` import 区（~235-239）。本任务在这些位置精确插入。

- [ ] **Step 1: 加 import**

在 `<script setup>` import 区加：

```ts
import QrCode from '@/components/QrCode/index.vue'
import { buildCardPayload } from '@/utils/qrPayload'
import QRCode from 'qrcode'
```

- [ ] **Step 2: 加行操作「二维码」按钮**

在行操作列（操作 `<el-table-column>` 内，现有「查看」按钮前）插入：

```vue
<el-button link type="primary" size="small" icon="Grid" @click="handleQrCode(scope.row)" v-hasPermi="['mes:pro:card:query']">二维码</el-button>
```

并把该 `<el-table-column>` 的 `width="160"` 改为 `width="220"`。

- [ ] **Step 3: 加工具栏「批量打印」按钮**

在工具栏「导出」按钮的 `</el-col>` 之后、`<right-toolbar />` 之前插入：

```vue
<el-col :span="1.5">
  <el-button type="info" plain icon="Printer" size="small" :disabled="multiple" @click="handleBatchPrint" v-hasPermi="['mes:pro:card:query']">批量打印</el-button>
</el-col>
```

- [ ] **Step 4: 加二维码弹窗**

在模板末尾（其它 `<el-dialog>` 之后）加：

```vue
<el-dialog title="流转卡二维码" v-model="qrOpen" width="340px" append-to-body>
  <div v-if="qrRow" style="text-align:center">
    <QrCode :payload="buildCardPayload(qrRow.cardCode)" :size="240" />
    <div style="margin-top:12px;font-size:18px;font-weight:bold">{{ qrRow.cardCode }}</div>
    <div style="color:#999;margin-top:4px">{{ qrRow.itemName }}</div>
  </div>
  <template #footer>
    <el-button @click="handlePrintOne" icon="Printer">打印</el-button>
    <el-button @click="qrOpen = false">关 闭</el-button>
  </template>
</el-dialog>
```

- [ ] **Step 5: 加 script 逻辑**

在 `<script setup>` refs 区加：

```ts
const qrOpen = ref(false)
const qrRow = ref<any>(null)
```

加函数：

```ts
function handleQrCode(row: any) {
  qrRow.value = row
  qrOpen.value = true
}

async function buildCardPrintHtml(rows: any[]): Promise<string> {
  const blocks = await Promise.all(rows.map(async c => {
    const dataUrl = await QRCode.toDataURL(buildCardPayload(c.cardCode), { width: 160, margin: 1 })
    return `<div class="card">
      <h2>${c.cardCode || ''}</h2>
      <img src="${dataUrl}" />
      <p>${c.itemName || ''} ${c.specification || ''}</p>
      <p>数量: ${c.quantityTransfered ?? ''} ${c.unitName || ''}</p>
      <p>当前工序: ${c.currentProcessName || ''}</p>
      <p>工单: ${c.workorderCode || ''}</p>
    </div>`
  }))
  return `<html><head><title>流转卡打印</title><style>
    body{margin:0;font-family:sans-serif}
    .sheet{display:flex;flex-wrap:wrap}
    .card{width:50%;box-sizing:border-box;padding:20px;border:1px dashed #bbb;text-align:center;page-break-inside:avoid}
    img{width:160px;height:160px} h2{margin:8px 0} p{margin:4px 0;font-size:14px;color:#333}
  </style></head><body><div class="sheet">${blocks.join('')}</div>
  <script>window.onload=function(){window.print()}<\/script></body></html>`
}

function openPrintWindow(html: string) {
  const w = window.open('', '_blank')
  if (!w) { proxy.$modal.msgError('请允许浏览器弹出窗口'); return }
  w.document.write(html)
  w.document.close()
}

async function handleBatchPrint() {
  if (!ids.value.length) { proxy.$modal.msgWarning('请勾选要打印的流转卡'); return }
  const rows = dataList.value.filter((r: any) => ids.value.includes(r.cardId))
  openPrintWindow(await buildCardPrintHtml(rows))
}

async function handlePrintOne() {
  if (qrRow.value) openPrintWindow(await buildCardPrintHtml([qrRow.value]))
}
```

- [ ] **Step 6: 浏览器实测（验证红线）**

`cd frontend && npm run dev`，登录 → 流转卡管理页：
1. 点某行「二维码」→ 弹窗显示二维码图 + 卡号。用手机扫该二维码应得到 `QXX|CARD|{卡号}`。
2. 勾选 2+ 行 → 点「批量打印」→ 新窗口弹出 A4 排版（每卡一块含二维码+信息）→ 触发打印预览。
3. 单卡弹窗内「打印」→ 同样弹打印窗。

Expected: 二维码可扫、打印窗正常。修掉任何 TS 报错（`npm run build` 或 IDE 检查）。

- [ ] **Step 7: Commit**

```bash
git add frontend/src/views/mes/pro/card/index.vue
git commit -m "feat(pro/card): 流转卡二维码弹窗 + 批量打印（A4）"
```

---

## Task 5: App 统一扫码分发 + 工作台「扫一扫」入口

**Files:**
- Create: `app/utils/qrPayload.js`
- Create: `app/utils/scanDispatch.js`
- Modify: `app/pages/work/index.vue`

**Interfaces:**
- Produces: `parseQrPayload(raw)`、`buildQrPayload(type,code)`、`scanAndDispatch()`（Promise，扫完按前缀跳页）。

- [ ] **Step 1: 创建 qrPayload.js**

Create `app/utils/qrPayload.js`：

```js
const PREFIX = 'QXX'
const TYPES = ['CARD', 'MAT', 'ROLL', 'WO', 'PKG']

export function buildQrPayload(type, code) {
  return PREFIX + '|' + type + '|' + code
}

export function parseQrPayload(raw) {
  if (!raw) return null
  const parts = raw.split('|', 3)
  if (parts.length < 3 || parts[0] !== PREFIX) return null
  if (TYPES.indexOf(parts[1]) < 0) return null
  if (!parts[2]) return null
  return { type: parts[1], code: parts[2] }
}
```

- [ ] **Step 2: 创建 scanDispatch.js**

Create `app/utils/scanDispatch.js`：

```js
import { parseQrPayload } from '@/utils/qrPayload'

function scan() {
  return new Promise((resolve, reject) => {
    uni.scanCode({
      onlyFromCamera: false,
      scanType: ['barCode', 'qrCode'],
      success: (res) => resolve(res.result),
      fail: (err) => reject(err)
    })
  })
}

/**
 * 扫码并按 payload 前缀分发到对应页面。
 * P1 仅落地 CARD/WO；MAT/ROLL 待 P2/P3。
 */
export async function scanAndDispatch() {
  let raw
  try {
    raw = await scan()
  } catch (e) {
    return null // 用户取消
  }
  const payload = parseQrPayload(raw)
  if (!payload) {
    // 非本系统码：当作裸工单/卡号，交报工页处理
    uni.navigateTo({ url: '/pages/mes/pro/report?rawCode=' + encodeURIComponent(raw) })
    return { type: 'RAW', code: raw }
  }
  switch (payload.type) {
    case 'CARD':
      uni.navigateTo({ url: '/pages/mes/pro/report?cardCode=' + encodeURIComponent(payload.code) })
      break
    case 'WO':
      uni.navigateTo({ url: '/pages/mes/pro/report?workorderCode=' + encodeURIComponent(payload.code) })
      break
    case 'MAT':
    case 'ROLL':
      uni.showToast({ icon: 'none', title: '该类型码待 P2/P3 支持' })
      break
    default:
      uni.showToast({ icon: 'none', title: '暂不支持该类型码' })
  }
  return payload
}
```

- [ ] **Step 3: 工作台加「扫一扫」入口**

**Read** `app/pages/work/index.vue`：找到「生产管理」grid block（`v-if="!isVendor"`，~70-113 行）内现有 `<uni-grid-item @click="goReport">`。在其后加一个 grid-item：

```vue
<uni-grid-item @click="goScan">
  <view class="grid-item-box">
    <uni-icons type="scan" size="30" color="#409eff"></uni-icons>
    <text class="text">扫一扫</text>
  </view>
</uni-grid-item>
```

在 `<script setup>` import 区加：

```js
import { scanAndDispatch } from '@/utils/scanDispatch'
```

在函数区加：

```js
async function goScan() {
  await scanAndDispatch()
}
```

> 若页面 uni-ui 走显式 import（见 `work/index.vue` 顶部 import 列表），`uni-icons` 已注册则无需额外处理；若报组件未注册，按该页既有显式 import 模式补 `uni-icons`。

- [ ] **Step 4: 真机/模拟器实测（验证红线）**

运行 App：`cd app && npm run dev:%APP%`（按平台）。在工作台点「扫一扫」：
1. 扫一张 P1 生成的流转卡二维码（PC 端 Task 4 打印/展示的）→ 跳转报工页。
2. 扫一个非本系统码（如随便一个 URL）→ 跳报工页带 rawCode。
3. 取消扫码 → 不跳转、不报错。

Expected: 分发正确。

- [ ] **Step 5: Commit**

```bash
git add app/utils/qrPayload.js app/utils/scanDispatch.js app/pages/work/index.vue
git commit -m "feat(app): 统一扫码分发 scanDispatch + 工作台扫一扫入口"
```

---

## Task 6: App 报工页 扫码报工 2 步流

**Files:**
- Create: `app/api/mes/pro/procard.js`
- Modify: `app/pages/mes/pro/report.vue`

**Interfaces:**
- Consumes: Task 2 的 `GET /mes/pro/procard/scan/{cardCode}`；Task 5 的 `parseQrPayload`（处理 rawCode 兜底）。

> 动手前 **Read** `app/pages/mes/pro/report.vue` 全文（544 行）确认：state（~176-191）、`searchWorkorder`（~244-279）、`selectTask`（~282-290）、`doSubmit`（~354-416）。本任务**新增** onLoad + scanByCard 路径，**保留**原 workorderCode 手输路径作 fallback，提交逻辑 `doSubmit` 不改。

- [ ] **Step 1: 创建 procard.js API**

Create `app/api/mes/pro/procard.js`：

```js
import request from '@/utils/request'

// 扫流转卡码反查报工上下文
export function getCardScanResult(cardCode) {
  return request({ url: '/mes/pro/procard/scan/' + encodeURIComponent(cardCode), method: 'get' })
}
```

- [ ] **Step 2: report.vue 加 import**

在 `<script setup>` import 区加：

```js
import { onLoad } from '@dcloudio/uni-app'
import { getCardScanResult } from '@/api/mes/pro/procard'
import { parseQrPayload } from '@/utils/qrPayload'
```

- [ ] **Step 3: 加 state**

在现有 state 区加：

```js
const card = ref(null)                       // 扫码得到的流转卡
const reportedQualifiedSum = ref(0)          // 该卡该工序已报合格数
```

- [ ] **Step 4: 加 onLoad + scanByCard + reasonText**

在 `<script setup>` 函数区加：

```js
onLoad((options) => {
  if (options.cardCode) {
    scanByCard(options.cardCode)
  } else if (options.workorderCode) {
    workorderCode.value = options.workorderCode
    searchWorkorder()
  } else if (options.rawCode) {
    // 裸码兜底：像卡号就当卡，否则当工单
    const parsed = parseQrPayload(options.rawCode)
    if (parsed && parsed.type === 'CARD') {
      scanByCard(parsed.code)
    } else {
      workorderCode.value = options.rawCode
      searchWorkorder()
    }
  }
})

function reasonText(reason) {
  const map = {
    CARD_NOT_FOUND: '未找到该流转卡',
    CARD_COMPLETED: '该流转卡已完工，无需报工',
    CARD_OUTSOURCING: '该流转卡外协中，请到外协收货页操作',
    CARD_SCRAPPED: '该流转卡已报废',
    NO_REPORTABLE_TASK: '当前工序无可报工任务'
  }
  return map[reason] || '当前不可报工'
}

function scanByCard(cardCode) {
  proxy.$modal.loading('查询流转卡...')
  getCardScanResult(cardCode).then(res => {
    proxy.$modal.closeLoading()
    const data = res.data || {}
    if (!data.card) {
      proxy.$modal.msgError('未找到流转卡：' + cardCode)
      return
    }
    if (!data.canReport) {
      proxy.$modal.alert(reasonText(data.reason), '无法报工')
      return
    }
    card.value = data.card
    // 用卡的冗余字段填充 workorder 供 doSubmit 复用
    workorder.value = {
      workorderId: data.card.workorderId,
      workorderCode: data.card.workorderCode,
      workorderName: data.card.workorderName,
      productId: data.card.itemId,
      productCode: data.card.itemCode,
      productName: data.card.itemName,
      unitOfMeasure: data.card.unitOfMeasure,
      unitName: data.card.unitName
    }
    taskList.value = data.reportableTasks || []
    outsourceTaskList.value = []
    reportedQualifiedSum.value = data.reportedQualifiedSum || 0
    // 单任务自动选中 → 直接进填数量步（4 步压成 2 步）
    if (taskList.value.length === 1) {
      selectTask(taskList.value[0])
    }
  }).catch(() => {
    proxy.$modal.closeLoading()
    proxy.$modal.msgError('查询流转卡失败')
  })
}
```

- [ ] **Step 5: 模板加流转卡标识条（可选，提升体验）**

在模板顶部（`<template>` 第一个根节点内、原 Step1 之前）加：

```vue
<view class="card-banner" v-if="card">
  <uni-tag text="流转卡报工" type="primary" size="small" />
  <text class="card-banner-code">{{ card.cardCode }} · {{ card.currentProcessName }}</text>
</view>
```

并在 SCSS 区加：

```scss
.card-banner { display:flex; align-items:center; gap:8px; padding:8px 12px; background:#ecf5ff; border-radius:6px; margin:8px 0; }
.card-banner-code { font-size:14px; color:#303133; }
```

- [ ] **Step 6: 真机/模拟器实测（验证红线）**

确保后端已重启（Task 2 Step 7）。运行 App：
1. 工作台「扫一扫」→ 扫一张 ACTIVE 流转卡码 → 报工页直接到「填数量」步（单任务自动选中），顶部显示「流转卡报工 · 卡号 · 工序」。
2. 填数量 → 提交 → 提示「报工提交成功」。
3. 扫一张已完工卡 → 弹「该流转卡已完工」。
4. 多任务卡（若存在）→ 显示任务列表让选。
5. 直接进报工页（不扫码，工作台「生产报工」入口）→ 原 workorderCode 手输流程仍可用。

Expected: 扫码 2 步报工通；手输 fallback 不破。后端 `/mes/pro/procard/scan/{cardCode}` 返回正确。

- [ ] **Step 7: Commit**

```bash
git add app/api/mes/pro/procard.js app/pages/mes/pro/report.vue
git commit -m "feat(app/report): 扫流转卡码 2 步报工（onLoad cardCode → scanForReport → 自动选任务）"
```

---

## 全链路验证清单（所有 Task 完成后）

- [ ] 后端单测：`cd backend && mvn -pl ruoyi-common,ruoyi-system -am test -Dtest=QrPayloadUtilTest,ProCardScanServiceUnitTest` 全绿。
- [ ] 后端重新打包重启，`/mes/pro/procard/scan/{真实卡号}` 实测返回结构正确。
- [ ] PC：流转卡页二维码弹窗可扫出 `QXX|CARD|卡号`；批量打印 A4 正常。
- [ ] App：工作台扫一扫 → 扫流转卡码 → 2 步报工提交成功。
- [ ] App：原手输工单报工路径未回归。
- [ ] 全程未改 workspace 其它项目文件；所有 SQL 走 factory_id 拦截器（无裸 SQL 绕过）。

## 自检（writing-plans self-review 已完成）
- **Spec 覆盖**：设计 §3（码体系→Task1/3/5）、§6.2（scanForReport→Task2）、§7.4（流转卡二维码弹窗→Task4）、§7.2（App 扫码分发→Task5）、§7.3（报工 4→2 步→Task6）、§8（A4 打印→Task4）。§4.5 编码非 bug 已在设计文档澄清，本计划无相关任务。
- **类型一致**：`scanForReport(String)->CardScanResultVO`、`buildCardPayload`、`parseQrPayload`、`getCardScanResult` 前后端命名一致。
- **无占位符**：每步含可执行代码/命令。
