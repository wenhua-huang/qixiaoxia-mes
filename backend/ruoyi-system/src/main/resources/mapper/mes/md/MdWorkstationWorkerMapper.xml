<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.mes.md.MdWorkstationWorkerMapper">

    <resultMap type="com.ruoyi.system.domain.mes.md.MdWorkstationWorker" id="MdWorkstationWorkerResult">
        <result property="recordId"       column="record_id"        />
        <result property="factoryId"      column="factory_id"       />
        <result property="workstationId"  column="workstation_id"   />
        <result property="userId"         column="user_id"          />
        <result property="userName"       column="user_name"        />
        <result property="roleType"       column="role_type"        />
        <result property="remark"         column="remark"           />
        <result property="createBy"       column="create_by"        />
        <result property="createTime"     column="create_time"      />
        <result property="updateBy"       column="update_by"        />
        <result property="updateTime"     column="update_time"      />
    </resultMap>

    <sql id="selectMdWorkstationWorkerVo">
        select record_id, factory_id, workstation_id, user_id, user_name,
               role_type, remark, create_by, create_time, update_by, update_time
        from qxx_md_workstation_worker
    </sql>

    <select id="selectMdWorkstationWorkerList" parameterType="com.ruoyi.system.domain.mes.md.MdWorkstationWorker" resultMap="MdWorkstationWorkerResult">
        <include refid="selectMdWorkstationWorkerVo"/>
        <where>
            <if test="workstationId != null"> and workstation_id = #{workstationId}</if>
            <if test="userId != null"> and user_id = #{userId}</if>
            <if test="userName != null  and userName != ''"> and user_name like concat('%', #{userName}, '%')</if>
            <if test="roleType != null  and roleType != ''"> and role_type = #{roleType}</if>
        </where>
    </select>

    <select id="selectMdWorkstationWorkerByRecordId" parameterType="Long" resultMap="MdWorkstationWorkerResult">
        <include refid="selectMdWorkstationWorkerVo"/>
        where record_id = #{recordId}
    </select>

    <insert id="insertMdWorkstationWorker" parameterType="com.ruoyi.system.domain.mes.md.MdWorkstationWorker" useGeneratedKeys="true" keyProperty="recordId">
        insert into qxx_md_workstation_worker
        <trim prefix="(" suffix=")" suffixOverrides=",">            <if test="workstationId != null">workstation_id,</if>
            user_id,
            <if test="userName != null">user_name,</if>
            <if test="roleType != null">role_type,</if>
            <if test="remark != null">remark,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">            <if test="workstationId != null">#{workstationId},</if>
            #{userId},
            <if test="userName != null">#{userName},</if>
            <if test="roleType != null">#{roleType},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateMdWorkstationWorker" parameterType="com.ruoyi.system.domain.mes.md.MdWorkstationWorker">
        update qxx_md_workstation_worker
        <trim prefix="SET" suffixOverrides=",">            <if test="workstationId != null">workstation_id = #{workstationId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="roleType != null">role_type = #{roleType},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where record_id = #{recordId}
    </update>

    <delete id="deleteMdWorkstationWorkerByRecordId" parameterType="Long">
        delete from qxx_md_workstation_worker where record_id = #{recordId}
    </delete>

    <delete id="deleteMdWorkstationWorkerByRecordIds" parameterType="String">
        delete from qxx_md_workstation_worker where record_id in
        <foreach item="recordId" collection="array" open="(" separator="," close=")">
            #{recordId}
        </foreach>
    </delete>

    <delete id="deleteMdWorkstationWorkerByWorkstationId" parameterType="Long">
        delete from qxx_md_workstation_worker where workstation_id = #{workstationId}
    </delete>
</mapper>
