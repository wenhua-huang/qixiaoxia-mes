<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.mes.md.MdItemAttrPaperMapper">
    <resultMap type="com.ruoyi.system.domain.mes.md.MdItemAttrPaper" id="Result">
        <result property="attrId" column="attr_id"/>
        <result property="factoryId" column="factory_id"/>
        <result property="itemId" column="item_id"/>
        <result property="paperWidth" column="paper_width"/>
        <result property="paperWeight" column="paper_weight"/>
        <result property="paperSource" column="paper_source"/>
        <result property="paperType" column="paper_type"/>
    </resultMap>

    <sql id="cols">attr_id,factory_id,item_id,paper_width,paper_weight,paper_source,paper_type,create_by,create_time,update_by,update_time,remark</sql>

    <select id="selectByItemId" parameterType="Long" resultMap="Result">
        select <include refid="cols"/> from qxx_md_item_attr_paper where item_id = #{itemId}
    </select>

    <insert id="insert" parameterType="com.ruoyi.system.domain.mes.md.MdItemAttrPaper" useGeneratedKeys="true" keyProperty="attrId">
        insert into qxx_md_item_attr_paper
        <trim prefix="(" suffix=")" suffixOverrides=",">            <if test="itemId != null">item_id,</if>
            <if test="paperWidth != null">paper_width,</if>
            <if test="paperWeight != null">paper_weight,</if>
            <if test="paperSource != null">paper_source,</if>
            <if test="paperType != null">paper_type,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">            <if test="itemId != null">#{itemId},</if>
            <if test="paperWidth != null">#{paperWidth},</if>
            <if test="paperWeight != null">#{paperWeight},</if>
            <if test="paperSource != null">#{paperSource},</if>
            <if test="paperType != null">#{paperType},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

    <update id="updateByItemId" parameterType="com.ruoyi.system.domain.mes.md.MdItemAttrPaper">
        update qxx_md_item_attr_paper
        <trim prefix="SET" suffixOverrides=",">
            <if test="paperWidth != null">paper_width = #{paperWidth},</if>
            <if test="paperWeight != null">paper_weight = #{paperWeight},</if>
            <if test="paperSource != null">paper_source = #{paperSource},</if>
            <if test="paperType != null">paper_type = #{paperType},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where item_id = #{itemId}
    </update>

    <delete id="deleteByItemId" parameterType="Long">
        delete from qxx_md_item_attr_paper where item_id = #{itemId}
    </delete>
</mapper>
